# Snapshot.js Configuration: Managed by the tool. Do not remove these lines.
# ADD_TO_GITIGNORE=FALSE
# USE_GITIGNORE=TRUE

# Check if the __snapshots__ directory is in the .gitignore file

# Use the .gitignore file as well as the .snapshotignore file

# This flag will add the __snapshots__ directory to the .gitignore file if it is not already present

# This flag will tell snapshot.js to use the .gitignore file as well as the .snapshotignore file

# Ignore snapshot-specific files
# NOTE: is there a good reason not to back these up?
#snapshot.js 
#.snapshotignore

# Ignore snapshot directory
__snapshots__/

# Ignore system and temp files
.DS_Store
Thumbs.db

# Ignore Node and environment config
node_modules/
.env
.env.local
npm-debug.log
package-lock.json

# IDE/project files
.vscode/
.idea/

# Build/dist outputs
dist/
build/
